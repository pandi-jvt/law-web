# Backend API base URL (no trailing slash).
# Set this to your backend URL in development and production.
# Build-time variable: set before `npm run build` for production deploy.
VITE_API_BASE_URL=http://localhost:9000
